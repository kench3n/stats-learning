Give a quick project health check:
1. Run `node tests/run_all.js` — report pass/fail
2. Run `git status` — report uncommitted changes
3. Run `git log --oneline -5` — show recent commits
Summarize everything concisely.
