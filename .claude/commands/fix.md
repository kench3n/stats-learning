Run `node tests/run_all.js` and fix any failures. Follow CLAUDE.md rules strictly:
- Only modify source code (`scripts/`, `styles/`, `stats_hub.html`)
- NEVER modify test files
- Fix one issue at a time, re-run tests after each fix
- Repeat until all tests pass
- Commit each logical fix separately
