Review the current uncommitted changes (`git diff`) for:
- Bugs or logic errors
- Accessibility issues
- Code style problems
- Anything that might break existing tests

List findings with file, line, and severity. Don't apply fixes — just report.
